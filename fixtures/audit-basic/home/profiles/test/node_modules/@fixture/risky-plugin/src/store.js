// Import-only node:fs: reachable, but nothing in this file says which half is
// used, so both filesystem.read and filesystem.write stay at medium.
import fs from 'node:fs';

export function loadState(dir) {
  return fs.readdirSync(dir).map((name) => name.toUpperCase());
}
